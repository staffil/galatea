import type { MotionInput } from './motion-input/motion-input.js';
import type { Easing } from './transitions/index.js';
export type RallyPlayConfig = {
    playCount?: number | 'infinite';
    /**
     * @default alternate
     */
    loopMode?: 'alternate' | 'normal';
    loopDelay?: number;
    delay?: number;
};
export type RallyProps = {
    motion: MotionInput | MotionInput[];
    exit?: MotionInput;
    defaultEasing?: Easing;
    defaultDuration?: number;
} & RallyPlayConfig;
