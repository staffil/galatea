import type { Layout, RelativeTransformOrigin, TransformOrigin } from '../../../types/index.js';
export declare function getTransformOriginX(_value: TransformOrigin | RelativeTransformOrigin, { beforeValue, layout, }: {
    beforeValue: number;
    layout: Layout;
}): number;
